import { TextFieldAndroidLeftMagnifyIcon, TextFieldIosLeftMagnifyIcon } from '#components';

export default platformed({
  fallback: TextFieldAndroidLeftMagnifyIcon,
  ios: TextFieldIosLeftMagnifyIcon,
});
